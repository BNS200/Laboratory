# Fractions

## Prerequisites
- Swich to project root
- Run `vcpkg install`

## Configure
- Swich to project root
- Run `cmake -B build -S .`

## Build
- Swich to project root
- Run `cmake --build build`

## Test
- Swich to project root
- Run `ctest --test-dir build`
